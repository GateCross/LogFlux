# ===========================================================================
# LogFlux frontend-next 环境变量
# Umi Max 约定：以 `UMI_APP_` 为前缀的变量会被注入到运行时 `process.env.UMI_APP_*`。
# （旧 Vue 版的 `VITE_*` → 新版 `UMI_APP_*`，映射见 design.md「环境变量映射」表）
# ===========================================================================

# ---- 部署基址（构建期） ----
# 子路径部署前缀（等价旧 VITE_BASE_URL）。未配置默认 `/`（Req 16.8）。
# 若部署在子目录，需以 `/` 结尾，如 `/admin/`。
UMI_APP_BASE_URL=/

# ---- 后端服务基址（Req 2.1 / 16.3 / 16.7） ----
# 等价旧 VITE_SERVICE_BASE_URL。留空表示采用同源相对路径（请求落到 `/api/*`）：
#  - 开发期：由 config/proxy.ts 将 `/api` 代理到后端（见下方 UMI_APP_PROXY_TARGET）
#  - 生产期：由 Caddy `reverse_proxy /api/*` 反代到后端
# 如需显式指定后端基址（即便与同源解析结果相同也以显式为准，Req 16.3），在此填写完整地址。
UMI_APP_SERVICE_BASE_URL=

# ---- 后端响应码表（运行时常量，可经环境变量覆盖；集中管理见 src/constants/service.ts） ----
# 成功码（等价旧 VITE_SERVICE_SUCCESS_CODE）。成功码集合 = { 此值, '0', '200' }。
UMI_APP_SERVICE_SUCCESS_CODE=200
# 登出码：收到后清空鉴权并跳登录页（Req 2.4）
UMI_APP_SERVICE_LOGOUT_CODES=3000,3001
# 弹窗登出码：收到后弹一次性错误框，确认后登出（Req 2.5）
UMI_APP_SERVICE_MODAL_LOGOUT_CODES=7777,7778
# 令牌过期码：收到后触发刷新并重放原请求（Req 2.6）
UMI_APP_SERVICE_EXPIRED_TOKEN_CODES=9999,9998,3333

# ---- 鉴权与路由 ----
# 动态路由模式（等价旧 VITE_AUTH_ROUTE_MODE）
UMI_APP_AUTH_ROUTE_MODE=dynamic
# 应用首页路由（等价旧 VITE_ROUTE_HOME）
UMI_APP_ROUTE_HOME=dashboard
# 超级角色：拥有该角色的用户放行所有 RBAC 鉴权（等价旧 VITE_STATIC_SUPER_ROLE，Req 4.5）
UMI_APP_STATIC_SUPER_ROLE=R_SUPER

# ---- 本地存储 ----
# 本地存储 key 前缀，用于隔离不同站点（等价旧 VITE_STORAGE_PREFIX）
UMI_APP_STORAGE_PREFIX=LF_

# ---- 开发代理目标（仅开发期，见 config/proxy.ts） ----
# 开发期 `/api` 代理到的后端地址。后端 go-zero 默认监听 8888。
UMI_APP_PROXY_TARGET=http://127.0.0.1:8888
