VITE_BASE=/

# 接口地址（生产环境用相对路径，由 Caddy 反代）
VITE_GLOB_API_URL=/api

# 是否开启压缩: none, brotli, gzip
VITE_COMPRESS=gzip

# 是否开启 PWA
VITE_PWA=false

# vue-router 的模式（全环境锁定 hash）
VITE_ROUTER_HISTORY=hash

# 是否注入全局 loading
VITE_INJECT_APP_LOADING=true

# 打包后是否生成 dist.zip
VITE_ARCHIVER=true
